{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the AI Usage Dock desktop window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default"
  ]
}
